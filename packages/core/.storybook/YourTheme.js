import { create } from '@storybook/theming';
import logo from './sundae-logo.svg';

export default create({
  base: 'light',
  brandTitle: 'Sundae Design System',
  brandUrl: 'https://sundae.com',
  brandImage: logo,
});
